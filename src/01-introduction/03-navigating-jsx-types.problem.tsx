export const Component = () => {
  return (
    <div
      // How do I figure out what type aria-posinset expects?
      aria-posinset={0}
      // How do I figure out what type onChange expects?
      onChange={(e)=>{}}

      // How do I get autocomplete with JSX?
    />
  );
};
      type a = React.FormEventHandler<HTMLDivElement>
